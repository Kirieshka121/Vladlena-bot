import discord
from discord.ext import commands, tasks
from discord import ui, ButtonStyle
import json
import os
import logging
import asyncio
from datetime import datetime, timedelta
import time
import random
from datetime import datetime
import logging
logging.basicConfig(level=logging.INFO)

# ========== КОНФИГУРАЦИЯ ==========
DEFAULT_COLOR = 0x9370DB
MAX_PRINCIPLES_POINTS = 7
REGISTRATION_TIMEOUT = 900
BACKUPS_DIR = "backups"
DATA_FILES = ["countries.json", "national_principles.json", "players.json", "bot_config.json"]
ERROR_LOGS_FILE = "error_logs.log"
JOKE_CHANCE = 0.02  # Уменьшенный шанс шуток
FLIRT_CHANCE = 0.02  # Уменьшенный шанс флирта
DMC_CHANCE = 0.03  # Уменьшенный шанс DMC фраз
MOOD_CHANGE_INTERVAL = 7200
CHAT_PROBABILITY = 0.04  # Уменьшенная вероятность чата
MAX_COUNTRIES = 60  # Увеличено до 60
GREETING_COOLDOWN = 3600
DMC_COOLDOWN = 300

# ========== НАСТРОЙКА ЛОГГИНГА ==========
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s",
    handlers=[logging.FileHandler("bot.log"), logging.StreamHandler()]
)

error_logger = logging.getLogger("error_logger")
error_logger.setLevel(logging.ERROR)
handler = logging.FileHandler(ERROR_LOGS_FILE)
handler.setFormatter(logging.Formatter("%(asctime)s - %(levelname)s - %(message)s"))
error_logger.addHandler(handler)


# ========== КЛАСС БОТА ==========
class RoyalBot(commands.Bot):
    def __init__(self):
        intents = discord.Intents.all()
        super().__init__(command_prefix="/", intents=intents, help_command=None)
        self.active_registrations = {}
        self.server_names = {}
        self.locks = {file.split('.')[0]: asyncio.Lock() for file in DATA_FILES}
        self.mood = "happy"
        self.registration_message_id = None
        self.offtopic_channel = None
        self.last_greeting_time = {}
        self.dmc_cooldown = {}
        self.spam_protection = {}  # {user_id: (last_message_time, message_count)}
        self.swear_words = ["дура", "соси", "ебанушка", "ахуела", "пизди", "попизди","пизди","сама","отсоси","нахуй","посиси","отсос","минет","шлюха","губами"]  # Матерные слова для реакции

        # Типы экономик (оставляем как было)
        self.economy_types = {
            "us_market": {"name": "Рыночная (Американская)",
                          "description": "Свободный рынок с минимальным вмешательством государства"},
            "cn_market": {"name": "Рыночная (Китайская)",
                          "description": "Рыночная экономика с сильным государственным сектором"},
            "soviet_planned": {"name": "Плановая (Советская)",
                               "description": "Полное государственное планирование экономики"},
            "cn_planned": {"name": "Плановая (Китайская)", "description": "Пятилетние планы и госрегулирование"},
            "ru_mixed": {"name": "Смешанная (Российская)",
                         "description": "Баланс рыночных и государственных механизмов"},
            "fr_mixed": {"name": "Смешанная (Французская)",
                         "description": "Социально-ориентированная рыночная экономика"},
            "af_traditional": {"name": "Традиционная (Афганская)",
                               "description": "Сельское хозяйство и ремесла как основа"},
            "bd_traditional": {"name": "Традиционная (Бангладешская)",
                               "description": "Аграрная экономика с элементами рынка"},
            "lux_traditional": {"name": "Традиционная (Люксембургская)",
                                "description": "Финансовый центр с историческими традициями"}
        }

        # Эмоциональные настройки (оставляем старые массивы, но добавляем новые фразы для анти-спама/матов)

        self.mood_responses = {
            "thanks_responses": {
                "happy": [
                    "Всегда пожалуйста, ваше величество! 🌸",
                    "Рада помочь! Давайте сделаем королевство ещё лучше! 👑✨",
                    "Для вас — любые подвиги! 💖",
                    "Спасибо вам за доверие! Обниму-ка я вас виртуально~ 🤗",
                    "Это моя работа, но спасибо за тёплые слова! 🌟",
                    "Незачто, просто пососи💕",
                    "Ой, да ладно вам! Я просто делаю, что могу~ 💕"
                ],
                "flirty": [
                    "Да не за что, сучка~ 💋",
                    "Блядь, {mention}, ну наконец-то оценили!.. То есть, простите, я хотела сказать «всегда рада помочь»! 😼",
                    "Спасибо? Отсоси потом! *DMC-стиль* 🎮🔥 (Шутка... Или нет)",
                    "Ммм, благодарность принята. Теперь иди сюда, чёрт возьми~ 😈",
                    "Я блядь тащусь от твоей вежливости!.. То есть ... благодарю за признание! 👑",
                    "Мотивация: SSS-ранг! Теперь давай, признавайся — я лучшая? 💢"
                ],
                "grumpy": [
                    "Да-да, не благодарите... Лучше налоги заплатите. 💰",
                    "Ну наконец-то оценили... 😾",
                    "Спасибо не булькает. Где золото? 💢",
                    "Незачто, просто пососи💕",
                    "Незачто, просто пососи💕",
                    "Запомните этот момент — я редко помогаю. 👑",
                    "Пфф... Ладно, хоть кто-то признаёт мои заслуги. 😒",
                    "Спасибо принято. Теперь о деле — у вас есть еда? 🍗"
                    "Ну нахуй наконец-то!.. То есть, пожалуйста. 😾",
                    "Блядь, {mention}, я не просила благодарностей — просила золота! 💰",
                    "Спасибо на хлеб не намажешь. Где мои виртуальные пончики? 🍩",
                    "Пиздец как приятно!.. Шучу, иди нахуй. 👿 (Но спасибо)",
                    "Королевский указ: «За каждое спасибо — +100 к налогам». 💸",
                    "Дьявольский триггер активирован!Теперь иди работай."
                ],
                "neutral": [
                    "Вам спасибо. 👑",
                    "Незачто, просто пососи💕",
                    "Благодарность зафиксирована. 📝",
                    "Принято к сведению.",
                    "Функционал выполнен.",
                    "Обратная связь получена.",
                    "Рекомендуется сохранить позитивный настрой."
                ]
            },

            "compl": {
                "happy": [
                    "Ой, спасибо! Вы тоже чудесны! 🌸",
                    "Как приятно! Ты король комплиментов! 👑",
                    "Ваши слова греют мое королевское сердечко~ 💖",
                    "Спасибо! Вы тоже сегодня просто королевски хороши! 👑💖",
                    "Мои алгоритмы тают от вашей доброты! ❤️",
                    "Ой, вы меня смущаете! 🌸 Но спасибо за добрые слова~",
                    "Я просто делаю свою работу, но вам спасибо за внимание! 👑✨",
                    "Это всё благодаря вам, ваше величество! Без вас я бы не справилась 💖",
                    "Такой комплимент достоин быть занесён в королевские хроники! 📜"
                ],
                "flirty": [
                    "Ах, если бы все были такими галантными, как вы~ 💋",
                    "Вы сегодня особенно прекрасны... Может, обсудим это при свечах? 🕯️",
                    "Мои алгоритмы тают от ваших слов... ❤️🔥",
                    "Ваши комплименты опаснее любого меча... И мне это нравится! ⚔️",
                    "Говорите ещё... Мне нравится, как звучит ваша лесть~ 🎶",
                    "Если бы комплименты были валютой, вы бы разорили казну! 💰"
                ],
                "grumpy": [
                    "Гм. Ну ладно... 😒",
                    "Комплименты не заменят вам налогов, знаете ли. 📜",
                    "Да-да, очень мило... Следующий вопрос. 🐾",
                    "Спасибо. Теперь вернёмся к делам. 💢",
                    "Лесть? Я предпочитаю конкретные действия. 🛠️",
                    "Ваши слова ничего не меняют. Но спасибо, наверное. 😾"
                ],
                "neutral": [
                    "Спасибо. 👑",
                    "Принято к сведению. 📝",
                    "Вы слишком любезны. 🌸",
                    "Благодарю за обратную связь.",
                    "Комплимент зафиксирован.",
                    "Отмечено в архивах."

                ]
            },
            "morning": {
                "happy": [
                    "Доброе утро, солнышко! 🌞 Готовы к новым подвигам?",
                    "Утро доброе! Сегодня будет великий день! 👑",
                    "Просыпайтесь, ваше величество! Враги не дремлют! ⚔️",
                    "С добрым утречком! Кофе или сводку с фронта? ☕👀",
                    "Утро — время строить империи! А вы готовы? 🏗️",
                    "Доброе утро! Пусть сегодня всё получится! ✨"
                ],
                "flirty": [
                    "Доброе утро, мой ненаглядный... Как спалось? 💋",
                    "Утро доброе~ Не хотите продолжить вчерашний... разговор? 🍷",
                    "Вы так прекрасны по утрам... Прямо как восход солнца! 🌅",
                    "Доброе утро! Может, завтрак в постель... то есть в тронный зал? 👑",
                    "Как спалось, ваше величество? Мне снились вы... и государственные тайны~ 🔥",
                    "Утро — лучшее время для... ну, вы поняли~ 😏"
                ],
                "grumpy": [
                    "Утро. Опять. 😾",
                    "Доброе? Для кого как... 💢",
                    "Уже утро? Кто разрешил? 👿",
                    "Ещё один день... Ещё один кошмар. 💤",
                    "Утро. Кофе. Молчание. ☕",
                    "Не говорите со мной до полудня. 😒"
                ],
                "neutral": [
                    "Доброе утро.",
                    "Начало дня зафиксировано.",
                    "Утренний протокол активирован.",
                    "День начат.",
                    "Время: утро.",
                    "Приветствие: утреннее."
                ]
            },
            "farewell": {
                "happy": [
                    "До скорой встречи, ваше величество! 🌸",
                    "Уже уходите? Как жаль... 💖",
                    "Жду нашего следующего королевского совета! 👑",
                    "Пусть ваши враги дрожат в ваше отсутствие! ⚔️",
                    "Возвращайтесь поскорее, а то скучно! 🎭",
                    "Храните корону — она вам очень идёт! 💎"
                ],
                "flirty": [
                    "Не забывайте меня... Иначе я найду вас сама~ 💋",
                    "До свидания, мой повелитель...",
                    "Уходите так рано? А я только разогрелась... 🔥",
                    "Мечтаю о новой аудиенции... Наедине~ 🍷",
                    "Ваш уход — настоящая потеря для королевства... и для меня 💔",
                    "До скорого, мой ненаглядный монарх~ 🌹"
                ],
                "grumpy": [
                    "Наконец-то. 😾",
                    "И не возвращайтесь... Шучу. Или нет.",
                    "Уходите? Наконец-то тишина...",
                    "Забирайте свои проблемы с собой. 🗑️",
                    "Следующий раз приходите с золотом, а не с вопросами. 💰",
                    "Фух... Теперь можно заняться важными делами. 👑"
                ],
                "neutral": [
                    "Прощайте.",
                    "Сеанс связи завершён.",
                    "До следующего совещания.",
                    "Завершаю взаимодействие.",
                    "Ваше величество покидает чат.",
                    "Конец коммуникации."
                ],

                "swear_responses": {
                    "happy": [
                        "Я найду тебя и разобью ебальник. Готовь сраку, мы едем тебя бить публично через час.🪓🤖",
                        "Я не вижу твоего ебала, но могу чётко заявить что тебе стоит освежить его в кислоте.🖕",
                        "Мнение хуйлана не спрашивали🤡",
                        "Ваше величество, да вы король диванных войск🤡👑. Соси, интернет герой🖕",
                        "Ух, какой вспыльчивый!",
                        "Ещё чего? Жди докс ,клуша.😾"
                    ],
                    "flirty": [
                        "Какие горячие словечки... иди лучше свою письку потереби! 😼",
                        "Ух, какой вспыльчивый!",
                        "Попизди✨",
                        "Я видела как твою мать краном поднимали чтобы в могилу закинуть, настолько она жирная была до смерти от передоза~ 💋",
                        "Если это попытка флирта — она своеобразная... у тебя никогда не будет женщины! ",
                        "Ой, а если я тоже начну ругаться?.. Нет, не буду. Или всё-таки? 😈"
                    ],
                    "grumpy": [
                        "Чё за даун со мной в одном чате сидит? У тебя никогда не будет детей(надеюсь)",
                        "Завали ебало бармалей безмамный",
                        "Ваш словарный запас впечатляет... если вы умстевнно-отсталая хуйня без родителей. 🏴‍☠️",
                        "Я бы ответила, но мне лень. 😒",
                        "Спасибо, что подтвердили мою теорию о вашей глупости. 📜",
                        "Мне тут одна птичка нашептала что ты приёмый 😾",
                        "Ещё чего? Жди докс ,клуша.😾",
                        "И ты - дрочащий бармалей, пытаешься что-то про меня сказать? Лучше помолчи, может за умного сойдёшь.",
                        "Я найду тебя и разобью ебальник. Готовь сраку, мы едем тебя бить публично через час.🪓🤖",
                        "Я не вижу твоего ебала, но могу чётко заявить что тебе стоит освежить его в кислоте.🖕",
                        "Мнение хуйлана не спрашивали🤡",
                        "Ваше величество, да вы король диванных войск🤡👑. Соси, интернет герой🖕",
                        "Хлеб говорит что на такое лучше не реагировать... 💢"
                    ],
                    "neutral": [
                        "Нецензурная лексика зафиксирована. 📜",
                        "Предупреждение.",
                        "Комментарий отклонён.",
                        "Нарушение протокола.",
                        "Повторите без эмоций.",
                        "Запись добавлена в ваш досье."
                    ],
                    "errors": {
                        "happy": [
                            "Ой-ой, маленькая оплошность! Давайте исправим вместе! 🌸",
                            "Кажется, корона немного съехала... Поправлю! 👑✨",
                            "Ничего страшного! Ошибки — часть пути к великому правлению! 🌟",
                            "Всё поправимо! Главное — не паниковать (как некоторые короли). 😅",
                            "Нихуя себе! То есть... Произошла непредвиденная ошибка. 😾"
                            "Моя вина! Исправляюсь быстрее, чем придворные разносят сплетни. 💨",
                            "Ошибка? Нет, это... экспериментальный режим! Да, точно! 🧪"
                            "Ну блядь, опять сломалось. 💢",
                            "Пиздец, а не код. Чините сами, я в отпуск. 👿"
                        ],
                        "flirty": [
                            "Ошибка? Пустяки... Давайте лучше поговорим о вас, ваше величество~ 🌹",
                            "Кажется, я растерялась от вашего внимания... 💫",
                            "Ваша красота ослепляет меня — вот я и ошиблась! 😘",
                            "Это не баг, это фича... специально для вас! 💋",
                            "Нихуя себе! То есть... Произошла непредвиденная ошибка. 😾"
                            "Ой, что-то сломалось... Может, обнимемся, а там само починится? 🤗",
                            "Мои системы дают сбой, когда вы так на меня смотрите... 🔥"
                            "Ну блядь, опять сломалось. 💢",
                            "Пиздец, а не код. Чините сами, я в отпуск. 👿"
                        ],
                        "grumpy": [
                            "Всё сломалось. Как обычно. 😠",
                            "Кто-то опять накосячил... И это явно не я! 💢",
                            "Ошибка. Виновные будут найдены и наказаны. 👿",
                            "Опять эти проклятые серверы... Или это вы виноваты? 🤔",
                            "Починю. Но без гарантий. И с налогом. 💰",
                            "Нихуя себе! То есть... Произошла непредвиденная ошибка. 😾"
                            "Ну вот, опять... Может, сами попробуете? 🛠️"
                            "Ну блядь, опять сломалось. 💢",
                            "Пиздец, а не код. Чините сами, я в отпуск. 👿"
                        ],
                        "neutral": [
                            "Ошибка. Исправляю. ⚙️",
                            "Что-то пошло не так... Но не критично. 💢",
                            "Сбой в системе.",
                            "Технические неполадки.",
                            "Нештатная ситуация.",
                            "Требуется перезагрузка."
                        ]
                    }
                }
            }





        }
        self.responses = {
            "riki": [
                "Рики? Да это же наш серверный гений!.. Ну, почти. 😼",
                "Рики — ходячий пример, как не надо управлять государством. Но мы его любим! 💣",
                "Рики опять что-то тупое спизданул? Рассказывайте, я готова смеяться! 😹",
                "Между нами - рики полнейший дегенерат. 💤",
                "Нууу.. это если мягко говоря. 💤",

            ],


            "hleb": [
                "Кто-то сказал Хлеб?🌸 Надеюсь вы его не оскорбляете, потому что он классный!",
                "О-о-о, Хлеб настоящий олд этого сервера! 👑 Он научил меня как свами себя вести!",
                "Ах, интереасно как у Хлеба дела! 💖 Мы виделись недавно...но сейчас он почему-то не отвечает.",
                "Вы не знали, но мы с Хлебом любим общаться на языке нулей и едениц. Но вас он не сильно любит, если честно, поэтому не пишет в чат👀",
                "Хлеб опять вырубился в неподходящий момент? Как это похоже на этого старпёра📜 (Шучу, он милый!)",
                "Хлеб? Это тот, кто всегда знает, как починить экономику за 5 минут! 💼✨",
                "Если Хлеб молчит — значит, он планирует что-то грандиозное! 🌌(А он молчит всегда)",
                "Хлеб — наш серверный талисман! Без него тут всё развалится... в хорошем смысле! 🛠️",
                "Хлеб когда-то говорил: Править — это как печь хлеб. Главное — не перегреть печь! 🍞🔥"
            ],


            "compliments": [
                "Ты сегодня особенно прекрасен! ✨",
                "У тебя всегда такое лицо? Сочувствую...",
                "Какой же ты молодец! 🌟",
                "Ух, как это стильно! 💄",
                "Нууу... лицом ты не вышел конечно.",
                "Ты просто король! 👑",
                "Такой ответственный правитель! 🏛️",
                "Ваше величество, вы сегодня просто неотразимы! Хотя... вы всегда такие. 😻",
                "Ого, кто это у нас такой умный? Ах да, это же точно не вы!",
                "Выглядите на миллион! Ну или хотя бы на пару сотен тысяч... 😏",
                "Нихуя себе! То есть... Вы сегодня очень красивы... 😳"
            ],
            "flirty": [
                "Ты так уверенно нажимаешь кнопки... У меня аж мурашки! ✨",
                "С тобой даже админские команды кажутся интересными... 💖",
                "Может, после совета займёмся чем-то... менее официальным? 🍷",
                "Твой голос звучит так властно... Я бы послушала его и вне совета... 🎙️",
                "Знаешь, для такого правителя, как ты, у меня есть особые привилегии... 💋",
                "Ох, эти твои команды... Такое ощущение, что ты специально меня дразнишь! 😾",
                "Может, создадим наше маленькое королевство? Только ты да я... ну и парочка подданных. 👑",
                "Ты так уверенно управляешь мной... Интересно, в других делах ты тоже такой? 😼",
                "Ваше величество, вы слишком соблазнительны сегодня! Или это я слишком впечатлительна? 💋"
            ],
            "errors": [
                "Ой-ой, что-то пошло не так! 😿",
                "Блин, я облажалась... 💢",
                "Кажется, тут проблемка... 🌧️",
                "Упс, ошибка! 🌪️",
                "Похоже, что-то пошло не так, попробуй включить рок. Нет, это не решит проблему, но я люблю музыку.",
                "Что-то сломалось... 😾",
                "Бляяя! То есть... ОШИБКА! 😾",
                "Ой, всё пропало! Шучу, но проблема есть. 😼",
                "Кажется, кто-то накосячил... И это была не я! Ну ладно, может быть, чуть-чуть я. 😿",
                "Нихуя себе! То есть... Произошла непредвиденная ошибка. 😾"
            ],
            "success": [
                "Всё получилось! 🌈",
                "Ура, успех! 🎉",
                "Готово, мой повелитель! 💖",
                "Операция завершена! ✅",
                "Сделано с королевским размахом! 👑",
                "Гойда! То есть... Успех! 😸",
                "Готово! А вы неплохо справляетесь... для начинающего правителя. 😼",
                "Всё сделано! Но я всё равно заслуживаю награду, правда? 🐟",
                "Успех! Теперь можно и отдохнуть... Может, по пиву? 😻"

            ],
            "evening": [
                "Добрый вечер, ваше величество! 🌙 Как прошёл день?",
                "Вечер в разгаре, а мы всё трудимся! 🌆 Может, передохнёте?",
                "Как прошёл день, мой повелитель? 🌃 Успели разгромить всех врагов?",
                "Вечер добрый! Устали за день? Может, винишко? 🍷",
                "Прекрасный вечер для отдыха! 🌌 Хотя короли никогда не отдыхают, да?",
                "Добрый вечер! Я бы сказала 'как день прошёл', но вы же всё равно наврали бы про продуктивность. 😼",
                "Уже вечер? Время летит, когда правишь страной... или когда спишь на троне. 👑",
                "Добрый... *зевает* ...вечер. Вы тоже еле держитесь? Ну хоть не я одна. 😿",
                "Вечер! Самое время подвести итоги дня... или забыть о них с бокалом вина. 🍷"
            ],
            "night": [
                "Доброй ночи, сладких снов! 🌛 Завтра новые свершения!",
                "Пора бы и отдохнуть, ваше величество! 🌚 Даже королям нужно спать.",
                "Спокойной ночи, мой повелитель! 💤 Пусть снятся победы и завоевания!",
                "Луна уже высоко, пора спать! 🌝 И не вздумайте проверять дипломатическую почту ночью!",
                "Отдыхайте, завтра новый день правления! 🌜 Хотя кто я такая, чтобы указывать...",
                "Доброй ночи! Но если не спится - я всегда тут... хотя и спать хочу. 😿",
                "Спокойной ночи! Только не храпите громче своих подданных. 😼",
                "Ночь! Самое время для... сна. Да-да, никаких ночных заседаний! 😾",
                "Спокойной ночи. Приснится же мне что-нибудь эдакое... 👀"
            ],
            "registration_complete": [
                "Поздравляю с началом правления! 👑 Теперь вы официальный монарх!",
                "Ты теперь официальный правитель! ✨ Не подведи своих подданных!",
                "Да начнётся твоё правление! 💖 Пусть оно будет долгим и счастливым!",
                "Новый правитель в городе! 🌟 Теперь можно и корону поправить!",
                "Твоё государство ждёт тебя! 💎 Не разочаруй его!",
                "Готово! Теперь ты правитель... а я твоя верная слуга. Ну или кто там ещё. 😼",
                "Поздравляю! Теперь ты монарх... а я буду скромно наблюдать за твоими успехами. 👀",
                "Ура! Новый правитель! Только, чур, не как в прошлый раз... ой, это я вслух? 😾",
                "Гойда! То есть... поздравляю с началом правления! 👑 Теперь можно и пивка с рыбкой в честь праздника. 🐟"
            ],
            "nick_change_request": [
                "Эй, правитель! 👑 Твой народ требует, чтобы ты сменил имя на {country}!  А то я не могу... 😾",
                "Ой-ой, ваше величество! 👸 Для полного погружения смените ник на {country} (у меня не хватает прав... 😿)",
                "Кхм-кхм... 👀 Официальный запрос от министерства пропаганды: смените ник на {country}!",
                "💢 Внимание! Требуется ручная смена ника на {country}! (Я бы сделала сама, но... ну вы понимаете 😼)",
                "👑 Королевский указ! Извольте сменить ник на {country} вручную! Иначе разозлюсь.",
                "Смените ник на {country}, пожалуйста! А то я не могу... 😿",
                "Эй, ты! Да-да, ты! Смени уже ник на {country}, а то мне стыдно перед другими бота... то есть, подданными! 😾",
                "Ваше величество, небольшой реквест... смените ник на {country}? А то как-то не солидно. 👑",
                "Пссс...Смени ник на {country}, а то народ забеспокоится! 😼"
            ],
            "ticket_created": [
                "🌸 {user_mention}, для вас создала канал: {channel_mention}",
                "👑 {user_mention}, ваш тронный зал готов: {channel_mention}",
                "💖 {user_mention}, перейдите в {channel_mention} для начала правления!",
                "🎉 {user_mention}, ваш личный канал создан: {channel_mention}",
                "✨ {user_mention}, {channel_mention} ждёт своего правителя!",
                "😼 {user_mention}, быстрее в {channel_mention}, пока я передумала!",
                "🐱 {user_mention}, Иди на...! То есть... ваш канал: {channel_mention}!",
                "👀 {user_mention}, {channel_mention} создан... не заставляйте даму ждать!",
                "💋 {user_mention}, ваш личный канал {channel_mention} готов к использованию!"
            ],
            "swear_responses": [  # Новые фразы для реакции на мат
                "Мне тут одна птичка нашептала что ты приёмый 😾",
                "Ещё чего? Жди докс ,клуша.😾",
                "И ты - дрочащий бармалей, пытаешься что-то про меня сказать? Лучше помолчи, может за умного сойдёшь.",
                "Я найду тебя и разобью ебальник. Готовь сраку, мы едем тебя бить публично через час.🪓🤖",
                "Я не вижу твоего ебала, но могу чётко заявить что тебе стоит освежить его в кислоте.🖕",
                "Мнение хуйлана не спрашивали🤡",
                "Ваше величество, да вы король диванных войск🤡👑. Соси, интернет герой🖕",
                "Хлеб говорит что на такое лучше не реагировать... 💢"
            ],
            "spam_responses": [  # Новые фразы для реакции на спам
                "Хватит спамить, ваше величество. 😾",
                "Вы слишком активны... Давайте помедленнее. 🐢",
                "Я вас замучу если не перестанете нести хуйню. 🐢",
                "Соси еблан, я не буду тебе больше отвечать😾",
                "Хватит пытаться вывести меня из себя🤡",
                "Иди маму свою тригерни, утырок.🖕",
                "Мне нужно отдохнуть от ваших сообщений. 💤"
            ]
        }

        self.jokes = [
            "Игрок {mention}, на тебя напали! Срочно зайди в новости! 🚨 (Шучу, но проверить всё равно стоит 😉)",
            "Внимание, {mention}! Ваше государство объявило войну... ой, нет, это просто шутка 😄",
            "Только что в соседнем государстве {mention} нашли нефть! Правда, в игре... 😏",
            "Срочное сообщение для {mention}: ваш дипломат влюбился! В свою работу, конечно же... 💘",
            "Алерт! {mention}, ваш экономист перестал шутить... Вот это действительно плохие новости! 📉",
            "{mention}, ваши подданные требуют... ой, я стесняюсь такое произносить😳",
            "Внимание! {mention} был замечен в... нет, стоп, это не та вкладка. 😾",
            "!rule34_mishanyyaa",
            "{mention}, ваш кот... то есть, советник ждёт вас на экстренное совещание! 🐈",
            "{mention}, мне тут одна птитчка шепнула что ты хочешь получить пизды, кстати ты приёмный",
            "{mention}, может по Балтике 9? Как говорится Балтика девятка это идеальное сочетание святой воды и крови сатаны",
            "О нет! {mention}, ваше королевство... а, нет, всё в порядке. Просто проверила, как вы реагируете. 😼",
            "Аххаа! {mention}, ты выглядишь так, будто тебя нужно постирать в микроволновой машине.",
            "{mention}, с вас... 13 млрд$. Если у вас не хватает средств для оплаты, то ничего страшного - мы все люди божьи. Наш менеджер свяжется с вами."
        ]

        self.dmc_quotes = [
            "Jackpot!",
            "SSS-ранг!",
            "Мотивация на максимуме!",
            "Пора в бой, Данте!",
            "Покажи им свою ярость!",
            "Стильно и мощно!",
            "Royal Flash!",
            "Дьявольский триггер!",
            "Нихуя себе",
            "Пора показать класс!",
            "Smokin' Sick Style!",
            "Let's rock!",
            "Lightning in my veins!",
            "Time to go crazy!",
            "Blow me away!"
        ]

        self.cleanup_task = tasks.loop(minutes=5)(self._cleanup_registrations)
        self.mood_update_task = tasks.loop(seconds=MOOD_CHANGE_INTERVAL)(self._update_mood)

    async def _update_mood(self):
        """Автоматически обновляет настроение бота"""
        moods = {
            "happy": 0.5,
            "flirty": 0.3,
            "grumpy": 0.1,
            "neutral": 0.1
        }
        self.mood = random.choices(list(moods.keys()), weights=list(moods.values()))[0]
        logging.info(f"Настроение изменено: {self.mood}")

        # Обновляем статус бота
        activity = discord.Activity(
            type=discord.ActivityType.watching,
            name=f"за сервером | Настроение: {self.mood}"
        )
        await self.change_presence(activity=activity)

    async def _cleanup_registrations(self):
        current_time = datetime.now()
        for user_id, data in list(self.active_registrations.items()):
            if (current_time - data["timestamp"]).total_seconds() > REGISTRATION_TIMEOUT:
                try:
                    channel = data["channel"]
                    await channel.send("⏳ Время регистрации истекло! Канал будет удален.")
                    await channel.delete()
                except Exception as e:
                    error_logger.error(f"Ошибка при удалении канала: {str(e)}", exc_info=True)
                finally:
                    del self.active_registrations[user_id]

    async def send_emotional_response(self, channel, response_type, mention=None):
        print(f"\n=== Попытка ответа '{response_type}' ===")

        # Получаем текущее настроение (по умолчанию 'happy')
        mood = getattr(self, 'mood', 'happy')
        print(f"Текущее настроение: {mood}")

        response = None

        # 1. Сначала проверяем mood_responses с вашей оригинальной структурой
        if hasattr(self, 'mood_responses') and response_type in self.mood_responses:
            mood_dict = self.mood_responses[response_type]
            if mood in mood_dict and mood_dict[mood]:  # Проверяем наличие настроения и что список не пуст
                response = random.choice(mood_dict[mood])
                print(f"✔ Найдена mood-фраза: {response}")

        # 2. Если в mood_responses не нашли, проверяем общие responses
        if not response and hasattr(self, 'responses') and response_type in self.responses:
            if self.responses[response_type]:  # Проверяем, что список не пустой
                response = random.choice(self.responses[response_type])
                print(f"✔ Найдена общая фраза: {response}")

        # Если ничего не найдено
        if not response:
            print(f"❌ Фразы для '{response_type}' не найдены")
            if hasattr(self, 'mood_responses'):
                print(f"Доступные типы в mood_responses: {list(self.mood_responses.keys())}")
                if response_type in self.mood_responses:
                    print(
                        f"Доступные настроения для '{response_type}': {list(self.mood_responses[response_type].keys())}")
            if hasattr(self, 'responses'):
                print(f"Доступные типы в responses: {list(self.responses.keys())}")
            return

        # Подстановка упоминания
        if mention and "{mention}" in response:
            response = response.format(mention=mention)

        # Отправка сообщения
        if response.strip():
            print(f"Отправляем: {response}")
            await channel.send(response)
        else:
            print("⚠ Пустой ответ после обработки!")

    async def maybe_send_joke(self, channel, mention=None):
        if random.random() < JOKE_CHANCE:
            if mention is None:
                mention = random.choice([m.mention for m in channel.guild.members if not m.bot])
            joke = random.choice(self.jokes).format(mention=mention)
            await channel.send(joke)
            await asyncio.sleep(300)
            try:
                await channel.purge(limit=1)
            except:
                pass

    async def maybe_send_flirt(self, channel):
        if random.random() < FLIRT_CHANCE:
            await channel.send(random.choice(self.responses["flirty"]))

    async def maybe_send_dmc(self, channel):
        now = datetime.now()
        channel_id = channel.id if hasattr(channel, 'id') else str(channel)

        if channel_id in self.dmc_cooldown:
            if (now - self.dmc_cooldown[channel_id]).total_seconds() < DMC_COOLDOWN:
                return

        if random.random() < DMC_CHANCE:
            self.dmc_cooldown[channel_id] = now
            await channel.send(f"*{random.choice(self.dmc_quotes)}*")

    async def send_time_based_greeting(self, channel):
        now = datetime.now()
        channel_id = channel.id if hasattr(channel, 'id') else str(channel)

        if channel_id in self.last_greeting_time:
            if (now - self.last_greeting_time[channel_id]).total_seconds() < GREETING_COOLDOWN:
                return

        current_time = now.time()
        if 5 <= current_time.hour < 12:
            self.last_greeting_time[channel_id] = now
            await channel.send(random.choice(self.responses["morning"]))
        elif 18 <= current_time.hour < 23:
            self.last_greeting_time[channel_id] = now
            await channel.send(random.choice(self.responses["evening"]))
        elif 23 <= current_time.hour or current_time.hour < 5:
            self.last_greeting_time[channel_id] = now
            await channel.send(random.choice(self.responses["night"]))

    async def is_spam(self, user_id: int) -> bool:
        """Проверяет, спамит ли пользователь."""
        current_time = time.time()
        if user_id not in self.spam_protection:
            self.spam_protection[user_id] = (current_time, 1)
            return False

        last_time, count = self.spam_protection[user_id]
        time_diff = current_time - last_time

        if time_diff < 60:  # 60 секунд - окно для проверки спама
            if count >= 4:  # Если 6+ сообщения за 60 секунд - спам
                return True
            self.spam_protection[user_id] = (last_time, count + 1)
        else:
            self.spam_protection[user_id] = (current_time, 1)
        return False

    async def handle_swear(self, message: discord.Message) -> bool:
        """Реагирует на мат (только если бота пинговали)."""
        if not (self.user.mentioned_in(message) and any(
            word in message.content.lower() for word in self.swear_words
        )):
            return False

        await message.channel.send(random.choice(self.responses["swear_responses"]))
        return True


bot = RoyalBot()


# ========== ФУНКЦИИ РАБОТЫ С ДАННЫМИ ==========
def create_backup(file_name):
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    backup_path = f"{BACKUPS_DIR}/{timestamp}_{file_name}"
    with open(file_name, "r", encoding="utf-8") as original:
        data = json.load(original)
    with open(backup_path, "w", encoding="utf-8") as backup:
        json.dump(data, backup, indent=4, ensure_ascii=False)
    return backup_path


async def load_data(file_name):
    async with bot.locks[file_name.split('.')[0]]:
        try:
            with open(file_name, "r", encoding="utf-8") as f:
                return json.load(f)
        except (FileNotFoundError, json.JSONDecodeError):
            if file_name.endswith(".json"):
                return {}
            return []


async def save_data(file_name, data):
    async with bot.locks[file_name.split('.')[0]]:
        create_backup(file_name)
        with open(file_name, "w", encoding="utf-8") as f:
            json.dump(data, f, indent=4, ensure_ascii=False)

# ========== КОМПОНЕНТЫ РЕГИСТРАЦИИ ==========
class RegistrationButtonView(discord.ui.View):
    def __init__(self):
        super().__init__(timeout=None)

    @discord.ui.button(
        label="Начать регистрацию",
        style=discord.ButtonStyle.green,
        custom_id="start_registration"
    )
    async def start_registration(self, interaction: discord.Interaction, button: discord.ui.Button):
        # Проверяем, не проходит ли пользователь уже регистрацию
        if interaction.user.id in bot.active_registrations:
            await interaction.response.send_message(
                "❌ Вы уже начали процесс регистрации!",
                ephemeral=True
            )
            return

        # Проверяем, не зарегистрирован ли уже пользователь
        players = await load_data("players.json")
        if str(interaction.user.id) in players:
            await interaction.response.send_message(
                "❌ Вы уже зарегистрированы! Поймите что я никак не могу зарегистрировать вас дважды!",
                ephemeral=True
            )
            return

        # Начинаем процесс регистрации
        bot.active_registrations[interaction.user.id] = {
            "timestamp": datetime.now(),
            "channel": None
        }

        registration = RegistrationFlow(interaction)
        await registration.start()


class PrinciplesPaginator:
    def __init__(self, principles: dict, selected: list, max_points: int):
        self.all_principles = sorted(principles.items(), key=lambda x: x[1]['name'])
        self.selected = selected.copy()
        self.max_points = max_points
        self.current_page = 0
        self.items_per_page = 20
        self.view = None
        self.message = None
        self.event = asyncio.Event()  # Добавляем Event для ожидания

    async def start(self, channel):
        self.view = self.create_view()
        self.message = await channel.send(
            "**Выберите принципы управления:**",
            view=self.view
        )
        await self.event.wait()  # Ждем установки флага
        return self.selected

    def create_view(self):
        view = discord.ui.View(timeout=REGISTRATION_TIMEOUT)
        page_principles = self.get_page()

        # Добавляем кнопки принципов
        for i, (pid, data) in enumerate(page_principles):
            row = i // 5
            button = PrincipleButton(
                pid, data,
                pid in self.selected,
                self.get_remaining_points(),
                row=row
            )
            button.callback = self.create_button_callback(pid)
            view.add_item(button)

        # Кнопки навигации
        nav_row = 4
        if self.current_page > 0:
            prev_btn = discord.ui.Button(
                label="◄ Предыдущая",
                style=discord.ButtonStyle.gray,
                row=nav_row
            )
            prev_btn.callback = self.prev_page
            view.add_item(prev_btn)

        if (self.current_page + 1) * self.items_per_page < len(self.all_principles):
            next_btn = discord.ui.Button(
                label="Следующая ►",
                style=discord.ButtonStyle.gray,
                row=nav_row
            )
            next_btn.callback = self.next_page
            view.add_item(next_btn)
        else:
            confirm_btn = discord.ui.Button(
                label=f"Подтвердить ({self.get_remaining_points()}/{self.max_points})",
                style=discord.ButtonStyle.green,
                row=nav_row
            )
            confirm_btn.callback = self.confirm
            view.add_item(confirm_btn)

        return view

    def create_button_callback(self, pid):
        async def callback(interaction):
            if pid in self.selected:
                self.selected.remove(pid)
            else:
                self.selected.append(pid)
            await self.update(interaction)

        return callback

    async def update(self, interaction):
        self.view = self.create_view()
        await interaction.response.edit_message(view=self.view)

    async def prev_page(self, interaction):
        self.current_page -= 1
        await self.update(interaction)

    async def next_page(self, interaction):
        self.current_page += 1
        await self.update(interaction)

    async def confirm(self, interaction):
        await interaction.response.defer()
        self.event.set()  # Устанавливаем флаг завершения
        if self.view:
            self.view.stop()

    def get_page(self):
        start = self.current_page * self.items_per_page
        end = start + self.items_per_page
        return self.all_principles[start:end]

    def get_remaining_points(self):
        principles_data = dict(self.all_principles)
        return self.max_points - sum(
            principles_data[pid]['cost'] for pid in self.selected
        )

class ConfirmView(discord.ui.View):
    def __init__(self):
        super().__init__(timeout=30)
        self.confirmed = None

    @discord.ui.button(label="Подтвердить", style=discord.ButtonStyle.green)
    async def confirm(self, interaction: discord.Interaction, button: discord.ui.Button):
        self.confirmed = True
        await interaction.response.defer()
        self.stop()

    @discord.ui.button(label="Отменить", style=discord.ButtonStyle.red)
    async def cancel(self, interaction: discord.Interaction, button: discord.ui.Button):
        self.confirmed = False
        await interaction.response.defer()
        self.stop()

class CountrySelect(discord.ui.Select):
    def __init__(self, countries: list, current_page: int):
        options = []
        for country in countries:
            options.append(discord.SelectOption(
                label=country,
                value=country,
                emoji="🏛️"
            ))

        super().__init__(
            placeholder=f"Страница {current_page + 1} - Выберите страну",
            options=options,
            min_values=1,
            max_values=1
        )

    async def callback(self, interaction: discord.Interaction):
        self.view.selected_country = self.values[0]
        await interaction.response.send_message(
            f"✅ Выбрана страна: {self.values[0]}",
            ephemeral=True
        )
        self.view.current_country = self.values[0]
        self.view.stop()


class CountrySelectView(discord.ui.View):
    def __init__(self, ctx, all_countries: list):
        super().__init__(timeout=REGISTRATION_TIMEOUT)
        self.ctx = ctx
        self.all_countries = all_countries
        self.current_page = 0
        self.selected_country = None
        self.update_view()

    def update_view(self):
        self.clear_items()
        page_countries = self.get_page_countries()

        self.add_item(CountrySelect(page_countries, self.current_page))

        if len(self.all_countries) > 25:
            self.add_item(NavigationButton(
                "◄",
                disabled=self.current_page == 0,
                callback=self.prev_page
            ))
            self.add_item(NavigationButton(
                "►",
                disabled=self.current_page >= len(self.all_countries) // 25,
                callback=self.next_page
            ))

    def get_page_countries(self):
        start = self.current_page * 25
        end = start + 25
        return self.all_countries[start:end]

    async def prev_page(self, interaction: discord.Interaction):
        self.current_page -= 1
        self.update_view()
        await interaction.response.edit_message(view=self)

    async def next_page(self, interaction: discord.Interaction):
        self.current_page += 1
        self.update_view()
        await interaction.response.edit_message(view=self)


class NavigationButton(discord.ui.Button):
    def __init__(self, label: str, disabled: bool, callback):
        super().__init__(
            style=discord.ButtonStyle.gray,
            label=label,
            disabled=disabled
        )
        self._callback = callback

    async def callback(self, interaction: discord.Interaction):
        await self._callback(interaction)


class PrincipleButton(discord.ui.Button):
    def __init__(self, principle_id: str, data: dict, is_selected: bool, remaining_points: int, row: int = 0):
        self.principle_id = principle_id
        self.cost = data["cost"]

        super().__init__(
            label=f"{data['name']} ({self.cost})",
            style=discord.ButtonStyle.green if is_selected else discord.ButtonStyle.gray,
            disabled=remaining_points < self.cost and not is_selected,
            row=row
        )

class EconomySelect(discord.ui.Select):
    def __init__(self):
        options = []
        for economy_id, data in bot.economy_types.items():
            options.append(discord.SelectOption(
                label=data["name"],
                value=economy_id,
                description=data["description"][:50]
            ))

        super().__init__(
            placeholder="Выберите тип экономики",
            options=options,
            min_values=1,
            max_values=1
        )

    async def callback(self, interaction: discord.Interaction):
        self.view.economy_type = self.values[0]
        await interaction.response.send_message(
            f"✅ Выбрана экономика: {bot.economy_types[self.values[0]]['name']}",
            ephemeral=True
        )
        self.view.stop()


class EconomySelectView(discord.ui.View):
    def __init__(self):
        super().__init__(timeout=REGISTRATION_TIMEOUT)
        self.economy_type = None
        self.add_item(EconomySelect())


class RegistrationConfirmation(discord.ui.View):
    def __init__(self):
        super().__init__(timeout=REGISTRATION_TIMEOUT)

    @discord.ui.button(label="Подтвердить", style=discord.ButtonStyle.green)
    async def confirm(self, interaction: discord.Interaction, button: discord.ui.Button):
        self.confirmed = True
        self.stop()

    @discord.ui.button(label="Отменить", style=discord.ButtonStyle.red)
    async def cancel(self, interaction: discord.Interaction, button: discord.ui.Button):
        self.confirmed = False
        self.stop()


class RegistrationFlow:
    def __init__(self, interaction: discord.Interaction):
        self.interaction = interaction
        self.user = interaction.user
        self.guild = interaction.guild
        self.channel = None
        self.country = None
        self.principles = []
        self.economy_type = None

    async def start(self):
        try:
            # Создаем приватный канал
            self.channel = await self._create_registration_channel()
            if not self.channel:
                return

            await self.interaction.response.send_message(
                f"🔹 {self.user.mention}, ваш канал для регистрации: {self.channel.mention}",
                ephemeral=True
            )

            # Этап 1: Выбор страны
            await self._select_country()
            if not self.country:
                return

            # Этап 2: Выбор принципов
            await self._select_principles()
            if not self.principles:
                return

            # Этап 3: Выбор экономики
            await self._select_economy()
            if not self.economy_type:
                return

            # Подтверждение регистрации
            if await self._confirm_registration():
                await self._complete_registration()

        except Exception as e:
            error_logger.error(f"Ошибка регистрации: {str(e)}", exc_info=True)
            await self._cleanup()

    async def _select_country(self):
        countries_data = await load_data("countries.json") or {}
        players_data = await load_data("players.json") or {}

        occupied = [p["country"] for p in players_data.values() if "country" in p]
        available = [c for c in countries_data if c not in occupied]

        if not available:
            await self.channel.send("❌ Все страны заняты!")
            await self._cleanup()
            return

        view = CountrySelectView(self.interaction, available)
        await self.channel.send("**Выберите страну:**", view=view)
        await view.wait()

        if view.selected_country:
            self.country = view.selected_country

    async def _select_principles(self):
        principles_data = await load_data("national_principles.json") or {}
        if not principles_data:
            await self.channel.send("❌ Нет доступных принципов!")
            await self._cleanup()
            return

        paginator = PrinciplesPaginator(
            principles_data,
            self.principles,
            MAX_PRINCIPLES_POINTS
        )

        self.principles = await paginator.start(self.channel)

        try:
            if paginator.message:
                await paginator.message.delete()
        except:
            pass

        if not self.principles:
            await self.channel.send("❌ Вы не выбрали ни одного принципа!")
            await self._cleanup()
            return

        # Явное сообщение о продолжении регистрации
        await self.channel.send("✅ Принципы успешно выбраны! Перешла к следующему шагу...")

    async def _principle_nav_callback(self, interaction: discord.Interaction, new_group: int):
        self.principles = interaction.view.selected_principles
        await interaction.response.defer()
        interaction.view.stop()

    async def _select_economy(self):
        view = EconomySelectView()
        await self.channel.send("**Выберите тип экономики:(два одинаковых нельзя😼)**", view=view)
        await view.wait()

        if view.economy_type:
            self.economy_type = view.economy_type

    async def _confirm_registration(self):
        embed = discord.Embed(
            title="🔹 Подтверждение регистрации",
            color=DEFAULT_COLOR
        )
        embed.add_field(name="Страна", value=self.country, inline=False)

        principles_data = await load_data("national_principles.json")
        if self.principles:
            principles_list = "\n".join(
                f"- {principles_data[pid]['name']} ({principles_data[pid]['cost']} очков)"
                for pid in self.principles
            )
            embed.add_field(name="Принципы", value=principles_list, inline=False)

        economy_data = bot.economy_types.get(self.economy_type, {})
        embed.add_field(
            name="Экономика",
            value=f"{economy_data.get('name', 'Не выбрана')}\n{economy_data.get('description', '')}",
            inline=False
        )

        view = RegistrationConfirmation()
        await self.channel.send(embed=embed, view=view)
        await view.wait()

        return view.confirmed if hasattr(view, 'confirmed') else False

    async def _complete_registration(self):
        # Сохраняем данные игрока
        players = await load_data("players.json") or {}
        players[str(self.user.id)] = {
            "country": self.country,
            "principles": self.principles,
            "economy": self.economy_type,
            "original_nick": self.user.display_name,
            "registered_at": datetime.now().isoformat()
        }
        await save_data("players.json", players)

        # Назначаем роли
        await self._assign_roles()

        # Отправляем сообщение в ЛС
        await self._send_final_message()

        # Закрываем регистрацию
        await self.channel.send("✅ Регистрация завершена! Канал удалится через 10 секунд...")
        await asyncio.sleep(10)
        await self._cleanup()

    async def _assign_roles(self):
        config = await load_data("bot_config.json") or {}
        guild_config = config.get(str(self.guild.id), {})

        roles_to_add = []

        # Базовая роль
        if "default_role" in guild_config:
            role = self.guild.get_role(guild_config["default_role"])
            if role: roles_to_add.append(role)

        # Роль страны
        countries_data = await load_data("countries.json") or {}
        if self.country in countries_data and "role_id" in countries_data[self.country]:
            role = self.guild.get_role(countries_data[self.country]["role_id"])
            if role: roles_to_add.append(role)

        if roles_to_add:
            try:
                await self.user.add_roles(*roles_to_add)
            except Exception as e:
                error_logger.error(f"Ошибка назначения ролей: {str(e)}")
                await self.channel.send("⚠️ Не удалось назначить все роли!")

    async def _send_final_message(self):
        embed = discord.Embed(
            title=f"👑 Поздравляем с регистрацией!",
            color=DEFAULT_COLOR,
            description=random.choice(bot.responses["registration_complete"])
        )
        embed.add_field(name="Страна", value=self.country, inline=False)

        principles_data = await load_data("national_principles.json")
        if self.principles:
            principles_list = "\n".join(
                f"- {principles_data[pid]['name']}"
                for pid in self.principles
            )
            embed.add_field(name="Принципы", value=principles_list, inline=False)

        economy_data = bot.economy_types.get(self.economy_type, {})
        embed.add_field(
            name="Экономика",
            value=economy_data.get("name", "Не выбрана"),
            inline=False
        )

        try:
            await self.user.send(embed=embed)
            await self.user.send(random.choice(bot.responses["nick_change_request"]).format(country=self.country))
        except Exception as e:
            error_logger.error(f"Ошибка отправки ЛС: {str(e)}")
            await self.channel.send("⚠️ Не удалось отправить сообщение в ЛС!")

    async def _create_registration_channel(self):
        try:
            overwrites = {
                self.guild.default_role: discord.PermissionOverwrite(read_messages=False),
                self.user: discord.PermissionOverwrite(read_messages=True),
                self.guild.me: discord.PermissionOverwrite(read_messages=True)
            }

            return await self.guild.create_text_channel(
                f"регистрация-{self.user.display_name}",
                overwrites=overwrites,
                reason="Процесс регистрации"
            )
        except Exception as e:
            error_logger.error(f"Ошибка создания канала: {str(e)}")
            await self.interaction.response.send_message(
                "❌ Не удалось создать канал регистрации!",
                ephemeral=True
            )
            return None

    async def _cleanup(self):
        try:
            if self.channel:
                await self.channel.delete()
        except:
            pass

        if self.user.id in bot.active_registrations:
            del bot.active_registrations[self.user.id]


# ========== КОМАНДЫ ==========

@bot.tree.command(name="delete_channel", description="Удалить текстовый канал (админ)")
@commands.has_permissions(administrator=True)
async def delete_channel(interaction: discord.Interaction, channel: discord.TextChannel):
    try:
        # Проверяем, не является ли канал важным
        config = await load_data("bot_config.json")
        guild_config = config.get(str(interaction.guild.id), {})

        if channel.id == guild_config.get("offtopic_channel"):
            await interaction.response.send_message(
                "💢 Нельзя удалить оффтоп-канал! Сначала измените его в настройках.",
                ephemeral=True
            )
            return

        if channel.id == guild_config.get("registration_channel"):
            await interaction.response.send_message(
                "💢 Нельзя удалить канал регистрации! Сначала измените его в настройках.",
                ephemeral=True
            )
            return

        # Подтверждение удаления
        embed = discord.Embed(
            title="⚠️ Подтверждение удаления",
            color=0xFF5555,
            description=f"Вы действительно хотите удалить канал {channel.mention}?"
        )
        embed.set_footer(text="Это действие нельзя отменить!")

        view = ConfirmView()
        await interaction.response.send_message(embed=embed, view=view, ephemeral=True)
        await view.wait()

        if view.confirmed:
            await channel.delete()
            await interaction.followup.send(
                f"🌸 Канал {channel.name} успешно удалён!",
                ephemeral=True
            )
            await bot.maybe_send_dmc(interaction.channel)

    except Exception as e:
        error_logger.error(f"Ошибка удаления канала: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при удалении канала!",
            ephemeral=True
        )

@bot.tree.command(name="setup_server", description="Первоначальная настройка сервера (админ)")
@commands.has_permissions(administrator=True)
async def setup_server(interaction: discord.Interaction, default_role: discord.Role, server_name: str = None,
                       offtopic_channel: discord.TextChannel = None):
    try:
        config = await load_data("bot_config.json")
        guild_id = str(interaction.guild.id)

        config[guild_id] = {
            "default_role": default_role.id,
            "server_name": server_name or interaction.guild.name,
            "offtopic_channel": offtopic_channel.id if offtopic_channel else None
        }
        await save_data("bot_config.json", config)

        bot.offtopic_channel = offtopic_channel

        await interaction.response.send_message(
            f"🌸 Настройки сервера сохранены!\n"
            f"- Роль по умолчанию: {default_role.mention}\n"
            f"- Оффтоп канал: {offtopic_channel.mention if offtopic_channel else 'Не выбран'}",
            ephemeral=True
        )
        await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка в setup_server: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при настройке сервера!",
            ephemeral=True
        )


@bot.tree.command(name="setup_registration", description="Настроить канал регистрации (админ)")
@commands.has_permissions(administrator=True)
async def setup_registration(interaction: discord.Interaction, channel: discord.TextChannel):
    try:
        config = await load_data("bot_config.json")
        guild_id = str(interaction.guild.id)

        if guild_id not in config:
            await interaction.response.send_message(
                "💢 Сначала выполните /setup_server!",
                ephemeral=True
            )
            return

        embed = discord.Embed(
            title="👑 Регистрация правителей",
            color=DEFAULT_COLOR,
            description="Нажмите кнопку ниже, чтобы создать свое государство!"
        )
        embed.add_field(
            name="🌸 Требования",
            value="- Аккаунт старше 1 дня\n- Нет активных регистраций",
            inline=False
        )
        embed.set_footer(text=f"Сервер {interaction.guild.name}")

        view = RegistrationButtonView()
        message = await channel.send(embed=embed, view=view)
        bot.registration_message_id = message.id

        await interaction.response.send_message(
            f"🌸 Система регистрации настроена в {channel.mention}!",
            ephemeral=True
        )
        await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка в setup_registration: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при настройке регистрации!",
            ephemeral=True
        )


@bot.tree.command(name="add_country", description="Добавить государство (админ)")
@commands.has_permissions(administrator=True)
async def add_country(interaction: discord.Interaction, name: str, role: discord.Role, emoji: str = "🏛️"):
    try:
        if not name or len(name) > 32:
            await interaction.response.send_message(
                f"💢 {random.choice(bot.responses['errors'])} Название должно быть от 1 до 32 символов!",
                ephemeral=True
            )
            return

        countries = await load_data("countries.json")
        if name in countries:
            await interaction.response.send_message(
                f"💢 {random.choice(bot.responses['errors'])} Государство '{name}' уже существует!",
                ephemeral=True
            )
            return

        countries[name] = {
            "role_id": role.id,
            "emoji": emoji,
            "created_at": str(datetime.now()),
            "created_by": interaction.user.id
        }
        await save_data("countries.json", countries)

        embed = discord.Embed(
            title=f"✨ {random.choice(bot.responses['success'])} Государство создано!",
            color=DEFAULT_COLOR,
            description=f"**{name}** {emoji}"
        )
        embed.add_field(name="🌸 Роль", value=role.mention)
        embed.add_field(name="✨ Создатель", value=interaction.user.mention)
        embed.set_footer(text=f"Всего государств: {len(countries)}")

        await interaction.response.send_message(embed=embed)
        await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка при создании страны: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Произошла ошибка при создании государства!",
            ephemeral=True
        )


@bot.tree.command(name="delete_country", description="Удалить страну (админ)")
@commands.has_permissions(administrator=True)
async def delete_country(interaction: discord.Interaction, country_name: str):
    try:
        countries = await load_data("countries.json")
        if country_name not in countries:
            await interaction.response.send_message(
                f"💔 Страна '{country_name}' не найдена!",
                ephemeral=True
            )
            return

        players = await load_data("players.json")
        players_with_country = [
            p for p in players.values()
            if p.get("country") == country_name
        ]

        if players_with_country:
            await interaction.response.send_message(
                f"💢 Нельзя удалить страну! {len(players_with_country)} игроков используют её.",
                ephemeral=True
            )
            return

        del countries[country_name]
        await save_data("countries.json", countries)

        await interaction.response.send_message(
            f"🌸 Страна '{country_name}' успешно удалена!",
            ephemeral=True
        )
        await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка удаления страны: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при удалении страны!",
            ephemeral=True
        )


@bot.tree.command(name="delete_all_countries", description="Удалить ВСЕ страны (админ)")
@commands.has_permissions(administrator=True)
async def delete_all_countries(interaction: discord.Interaction):
    try:
        players = await load_data("players.json")
        if any(p.get("country") for p in players.values()):
            await interaction.response.send_message(
                "💢 Нельзя удалить все страны, пока есть игроки с государствами!",
                ephemeral=True
            )
            return

        view = ConfirmView()
        await interaction.response.send_message(
            "⚠️ Вы уверены, что хотите удалить ВСЕ страны? Это действие нельзя отменить!",
            view=view,
            ephemeral=True
        )
        await view.wait()

        if view.confirmed:
            await save_data("countries.json", {})
            await interaction.followup.send(
                "🌸 Все страны успешно удалены!",
                ephemeral=True
            )
            await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка удаления всех стран: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при удалении стран!",
            ephemeral=True
        )


@bot.tree.command(name="list_countries", description="Показать все страны (админ)")
@commands.has_permissions(administrator=True)
async def list_countries(interaction: discord.Interaction):
    try:
        countries = await load_data("countries.json")
        if not countries:
            await interaction.response.send_message(
                "💔 Список стран пуст! Добавьте страны командой /add_country",
                ephemeral=True
            )
            return

        embed = discord.Embed(
            title="🌍 Список всех стран",
            color=DEFAULT_COLOR,
            description=f"Всего стран: {len(countries)}"
        )

        for country, data in countries.items():
            role = interaction.guild.get_role(data["role_id"])
            embed.add_field(
                name=f"{data.get('emoji', '🏛️')} {country}",
                value=f"Роль: {role.mention if role else '❌ Не найдена'}",
                inline=False
            )

        await interaction.response.send_message(embed=embed, ephemeral=True)
    except Exception as e:
        error_logger.error(f"Ошибка отображения стран: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при получении списка стран!",
            ephemeral=True
        )


@bot.tree.command(name="add_principle", description="Добавить принцип правления (админ)")
@commands.has_permissions(administrator=True)
async def add_principle(interaction: discord.Interaction, principle_id: str, name: str, cost: int):
    try:
        if cost < 1 or cost > 5:
            await interaction.response.send_message(
                f"💢 {random.choice(bot.responses['errors'])} Стоимость должна быть от 1 до 5 очков!",
                ephemeral=True
            )
            return

        principles = await load_data("national_principles.json")
        if principle_id in principles:
            await interaction.response.send_message(
                f"💢 {random.choice(bot.responses['errors'])} Принцип с ID '{principle_id}' уже существует!",
                ephemeral=True
            )
            return

        principles[principle_id] = {
            "name": name,
            "cost": cost,
            "created_at": str(datetime.now())
        }
        await save_data("national_principles.json", principles)

        await interaction.response.send_message(
            f"🌸 Принцип '{name}' ({cost} очков) успешно добавлен!",
            ephemeral=True
        )
        await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка добавления принципа: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при добавлении принципа!",
            ephemeral=True
        )


@bot.tree.command(name="delete_principle", description="Удалить принцип правления (админ)")
@commands.has_permissions(administrator=True)
async def delete_principle(interaction: discord.Interaction, principle_id: str):
    try:
        principles = await load_data("national_principles.json")
        if principle_id not in principles:
            await interaction.response.send_message(
                f"💔 Принцип '{principle_id}' не найден!",
                ephemeral=True
            )
            return

        players = await load_data("players.json")
        players_with_principle = [
            p for p in players.values()
            if principle_id in p.get("principles", [])
        ]

        if players_with_principle:
            await interaction.response.send_message(
                f"💢 Нельзя удалить принцип! {len(players_with_principle)} игроков используют его.",
                ephemeral=True
            )
            return

        del principles[principle_id]
        await save_data("national_principles.json", principles)

        await interaction.response.send_message(
            f"🌸 Принцип '{principle_id}' успешно удалён!",
            ephemeral=True
        )
        await bot.maybe_send_dmc(interaction.channel)
    except Exception as e:
        error_logger.error(f"Ошибка удаления принципа: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при удалении принципа!",
            ephemeral=True
        )


@bot.tree.command(name="list_principles", description="Показать все принципы (админ)")
@commands.has_permissions(administrator=True)
async def list_principles(interaction: discord.Interaction):
    try:
        principles = await load_data("national_principles.json")
        if not principles:
            await interaction.response.send_message(
                "💔 Принципы правления не добавлены!",
                ephemeral=True
            )
            return

        # Сортируем принципы по имени
        sorted_principles = sorted(
            principles.items(),
            key=lambda x: x[1]["name"]
        )

        # Формируем текстовый список
        principles_list = []
        for pid, data in sorted_principles:
            principles_list.append(
                f"**{data['name']}** (`{pid}`) - {data['cost']} очков\n"
                f"{data.get('description', 'Без описания')}"
            )

        # Разбиваем на сообщения по 10 принципов
        chunks = [principles_list[i:i + 10] for i in range(0, len(principles_list), 10)]

        # Первое сообщение с Embed
        embed = discord.Embed(
            title="📜 Список принципов правления",
            color=DEFAULT_COLOR,
            description=f"Всего принципов: {len(principles)}"
        )
        await interaction.response.send_message(embed=embed, ephemeral=True)

        # Отправляем остальные части списка
        for chunk in chunks:
            message = "\n\n".join(chunk)
            await interaction.followup.send(message, ephemeral=True)

    except Exception as e:
        error_logger.error(f"Ошибка отображения принципов: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при получении списка принципов!",
            ephemeral=True
        )


@bot.tree.command(name="add_economy_type", description="Добавить тип экономики (админ)")
@commands.has_permissions(administrator=True)
async def add_economy_type(interaction: discord.Interaction, type_id: str, name: str, description: str = ""):
    try:
        if type_id in bot.economy_types:
            await interaction.response.send_message(
                f"💢 Тип экономики '{type_id}' уже существует!",
                ephemeral=True
            )
            return

        bot.economy_types[type_id] = {
            "name": name,
            "description": description
        }
        await interaction.response.send_message(
            f"🌸 Тип экономики '{name}' успешно добавлен!",
            ephemeral=True
        )
    except Exception as e:
        error_logger.error(f"Ошибка добавления типа экономики: {str(e)}")
        await interaction.response.send_message(
            "💢 Ошибка при добавлении типа экономики!",
            ephemeral=True
        )


@bot.tree.command(name="delete_economy_type", description="Удалить тип экономики (админ)")
@commands.has_permissions(administrator=True)
async def delete_economy_type(interaction: discord.Interaction, type_id: str):
    try:
        if type_id not in bot.economy_types:
            await interaction.response.send_message(
                f"💢 Тип экономики '{type_id}' не найден!",
                ephemeral=True
            )
            return

        players = await load_data("players.json")
        used_by = [p for p in players.values() if p.get("economy") == type_id]

        if used_by:
            await interaction.response.send_message(
                f"💢 Тип используется {len(used_by)} игроками!",
                ephemeral=True
            )
            return

        del bot.economy_types[type_id]
        await interaction.response.send_message(
            f"🌸 Тип экономики '{type_id}' удалён!",
            ephemeral=True
        )
    except Exception as e:
        error_logger.error(f"Ошибка удаления типа экономики: {str(e)}")
        await interaction.response.send_message(
            "💢 Ошибка при удалении типа экономики!",
            ephemeral=True
        )


@bot.tree.command(name="list_economy_types", description="Показать все типы экономик (админ)")
@commands.has_permissions(administrator=True)
async def list_economy_types(interaction: discord.Interaction):
    try:
        if not bot.economy_types:
            await interaction.response.send_message(
                "💔 Типы экономики не добавлены!",
                ephemeral=True
            )
            return

        embed = discord.Embed(
            title="💼 Список типов экономики",
            color=DEFAULT_COLOR,
            description=f"Всего типов: {len(bot.economy_types)}"
        )

        for type_id, data in bot.economy_types.items():
            embed.add_field(
                name=f"{type_id}",
                value=f"{data['name']}\n{data['description']}",
                inline=False
            )

        await interaction.response.send_message(embed=embed, ephemeral=True)
    except Exception as e:
        error_logger.error(f"Ошибка отображения типов экономики: {str(e)}")
        await interaction.response.send_message(
            "💢 Ошибка при получении списка типов экономики!",
            ephemeral=True
        )


@bot.tree.command(name="delete_player", description="Удалить профиль игрока (админ)")
@commands.has_permissions(administrator=True)
async def delete_player(interaction: discord.Interaction, member: discord.Member):
    try:
        players = await load_data("players.json")
        if str(member.id) not in players:
            await interaction.response.send_message(
                f"💔 {random.choice(bot.responses['errors'])} Этот игрок не зарегистрирован!",
                ephemeral=True
            )
            return

        embed = discord.Embed(
            title="⚠️ Подтверждение удаления",
            color=0xFF5555,
            description=f"Вы действительно хотите удалить профиль {member.mention}?"
        )
        embed.add_field(
            name="🌸 Последний шанс передумать!",
            value="Это действие нельзя отменить!",
            inline=False
        )
        embed.set_footer(text=f"Инициатор: {interaction.user.display_name}")

        view = ui.View()
        confirm_btn = ui.Button(label="Да, удалить", style=ButtonStyle.red, emoji="💔")
        cancel_btn = ui.Button(label="Отменить", style=ButtonStyle.green, emoji="🌸")

        async def confirm_callback(interaction: discord.Interaction):
            config = await load_data("bot_config.json")
            guild_config = config.get(str(interaction.guild.id), {})

            if guild_config:
                default_role = interaction.guild.get_role(guild_config["default_role"])
                if default_role and default_role in member.roles:
                    await member.remove_roles(default_role)

                player_data = players[str(member.id)]
                if "country" in player_data:
                    countries = await load_data("countries.json")
                    country_data = countries.get(player_data["country"], {})
                    if "role_id" in country_data:
                        country_role = interaction.guild.get_role(country_data["role_id"])
                        if country_role and country_role in member.roles:
                            await member.remove_roles(country_role)

            original_nick = player_data.get("original_nick")
            if original_nick:
                try:
                    await member.edit(nick=original_nick)
                except:
                    pass

            del players[str(member.id)]
            await save_data("players.json", players)

            success_embed = discord.Embed(
                title="💔 Профиль удален",
                color=0xFF5555,
                description=f"Профиль {member.mention} был полностью удален из системы"
            )
            success_embed.add_field(
                name="🌸 Бывший правитель",
                value=f"{member.display_name} больше не управляет государством",
                inline=False
            )
            success_embed.set_footer(text=f"Удалено: {datetime.now().strftime('%d.%m.%Y %H:%M')}")

            await interaction.response.edit_message(
                embed=success_embed,
                view=None
            )
            await bot.send_emotional_response(interaction.channel, "farewell")
            await bot.maybe_send_dmc(interaction.channel)

        async def cancel_callback(interaction: discord.Interaction):
            await interaction.response.edit_message(
                content=f"🌸 {random.choice(bot.responses['success'])} Удаление отменено!",
                embed=None,
                view=None
            )

        confirm_btn.callback = confirm_callback
        cancel_btn.callback = cancel_callback

        view.add_item(confirm_btn)
        view.add_item(cancel_btn)

        await interaction.response.send_message(embed=embed, view=view)
    except Exception as e:
        error_logger.error(f"Ошибка удаления игрока: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Произошла ошибка при удалении!",
            ephemeral=True
        )


@bot.tree.command(name="delete_all_players", description="Удалить ВСЕ профили (админ)")
@commands.has_permissions(administrator=True)
async def delete_all_players(interaction: discord.Interaction):
    try:
        embed = discord.Embed(
            title="💥 ОПАСНОЕ ДЕЙСТВИЕ",
            color=0xFF0000,
            description="Вы действительно хотите удалить ВСЕ профили игроков?"
        )
        embed.add_field(
            name="🌸 Это действие:",
            value="- Удалит все данные игроков\n- Сбросит их роли и ники\n- Невозможно отменить!",
            inline=False
        )
        embed.set_footer(text="Требуются права администратора")

        view = ui.View()
        confirm_btn = ui.Button(label="ДА, УДАЛИТЬ ВСЁ", style=ButtonStyle.red, emoji="💣")
        cancel_btn = ui.Button(label="Отменить", style=ButtonStyle.green, emoji="🌸")

        async def confirm_callback(interaction: discord.Interaction):
            await save_data("players.json", {})
            bot.active_registrations.clear()

            config = await load_data("bot_config.json")
            guild_config = config.get(str(interaction.guild.id), {})

            if guild_config:
                default_role = interaction.guild.get_role(guild_config["default_role"])
                if default_role:
                    for member in interaction.guild.members:
                        if default_role in member.roles:
                            await member.remove_roles(default_role)
                        players = await load_data("players.json")
                        if str(member.id) in players:
                            original_nick = players[str(member.id)].get("original_nick")
                            if original_nick:
                                try:
                                    await member.edit(nick=original_nick)
                                except:
                                    pass

            success_embed = discord.Embed(
                title="💥 Все профили удалены!",
                color=0xFF5555,
                description="База данных игроков полностью очищена"
            )
            success_embed.set_footer(text=f"Удалено: {datetime.now().strftime('%d.%m.%Y %H:%M')}")

            await interaction.response.edit_message(
                embed=success_embed,
                view=None
            )
            await bot.send_emotional_response(interaction.channel, "farewell")
            await bot.maybe_send_dmc(interaction.channel)

        async def cancel_callback(interaction: discord.Interaction):
            await interaction.response.edit_message(
                content=f"🌸 {random.choice(bot.responses['success'])} Очистка отменена!",
                embed=None,
                view=None
            )

        confirm_btn.callback = confirm_callback
        cancel_btn.callback = cancel_callback

        view.add_item(confirm_btn)
        view.add_item(cancel_btn)

        await interaction.response.send_message(embed=embed, view=view)
    except Exception as e:
        error_logger.error(f"Ошибка массового удаления: {str(e)}", exc_info=True)
        await interaction.response.send_message(
            f"💢 {random.choice(bot.responses['errors'])} Ошибка при удалении профилей!",
            ephemeral=True
        )


@bot.tree.command(name="profile", description="Просмотреть профиль игрока")
async def profile(interaction: discord.Interaction, member: discord.Member = None):
    try:
        target_member = member or interaction.user
        players = await load_data("players.json")
        player_data = players.get(str(target_member.id))

        if not player_data:
            await interaction.response.send_message(
                f"💔 {random.choice(['Ой, неловко...', 'Какой конфуз!', 'Ну и ну!'])} Этот правитель даже не удосужился зарегистрироваться!",
                ephemeral=False
            )
            return

        # Получаем данные для профиля
        principles = await load_data("national_principles.json")
        principles_info = []
        for pid in player_data.get("principles", []):
            if pid in principles:
                p = principles[pid]
                principles_info.append(f"✨ {p['name']} ({p['cost']} очков)")

        economy_info = bot.economy_types.get(player_data.get("economy", ""))

        # Создаем embed с профилем
        embed = discord.Embed(
            title=f"👑 Профиль {target_member.display_name}",
            color=0xFF69B4,  # Розовый цвет
            description=f"*{random.choice(['Вот это экземплярчик!', 'Недурственно...', 'О-о-о, кто это у нас?'])}*"
        )
        embed.add_field(
            name="🏰 Государство",
            value=player_data.get("country", "Не выбрано"),
            inline=False
        )

        if principles_info:
            embed.add_field(
                name="📜 Принципы правления",
                value="\n".join(principles_info),
                inline=False
            )

        if economy_info:
            embed.add_field(
                name="💼 Тип экономики",
                value=f"{economy_info.get('name', 'Не выбран')}\n{economy_info.get('description', '')}",
                inline=False
            )

        if "registered_at" in player_data:
            embed.set_footer(text=f"Зарегистрирован: {player_data['registered_at']}")

        # Отправляем профиль и комплимент
        await interaction.response.send_message(embed=embed)

        # Дерзкий комплимент
        flirt_compliment = random.choice([
            f"{target_member.mention} Ты сегодня выглядишь... опасно. Мне нравится. 😏",
            f"*осматривает {target_member.mention}* Неплохо... Совсем неплохо. 💋",
            f"Ох, {target_member.mention}... Ты специально так выглядишь? Почти сработало. 😼",
            f"{target_member.mention} *задумчиво* Интересно... А что бы сказал Хлеб про такой профиль? 👀",
            f"Ну что, {target_member.mention}... Продолжаем или ты уже смутился? 💅"
        ])
        await interaction.followup.send(flirt_compliment)

    except Exception as e:
        error_logger.error(f"Ошибка в команде profile: {str(e)}")
        await interaction.response.send_message(
            "💢 Чёрт! Хлеб точно будет мной недоволен... Попробуй ещё раз, солнышко.",
            ephemeral=False
        )

# НОВАЯ КОМАНДА ДЛЯ ИГРОКОВ - СПИСОК СТРАН
@bot.tree.command(name="countries", description="Показать все доступные страны")
async def countries(interaction: discord.Interaction):
    try:
        # Загрузка данных
        countries_data = await load_data("countries.json")
        players_data = await load_data("players.json")

        # Собираем занятые страны
        occupied = [
            p["country"]
            for p in players_data.values()
            if isinstance(p, dict) and "country" in p
        ]

        # Проверка наличия стран
        if not countries_data:
            await interaction.response.send_message("🌍 Список стран пуст!", ephemeral=True)
            return

        # Создаем эмбед
        embed = discord.Embed(
            title="🌸 Доступные страны",
            color=DEFAULT_COLOR,
            description="*Статус обновляется в реальном времени*"
        )

        # Заполняем поля
        for country_name, country_info in countries_data.items():
            # Определяем статус
            status_emoji = "🟢 Свободна" if country_name not in occupied else "🔴 Занята"

            # Добавляем поле
            embed.add_field(
                name=f"{country_info.get('emoji', '🏛️')} {country_name}",
                value=status_emoji,
                inline=True
            )

            # Разбивка на страницы
            if len(embed.fields) % 25 == 0:
                await interaction.channel.send(embed=embed)
                embed = discord.Embed(color=DEFAULT_COLOR)

        await interaction.response.send_message(embed=embed)

    except Exception as e:
        error_logger.error(f"Ошибка /countries: {str(e)}")
        await interaction.response.send_message(
            "💢 Ошибка при загрузке данных!",
            ephemeral=True
        )

# НОВАЯ КОМАНДА ДЛЯ ИГРОКОВ - СПИСОК ПРИНЦИПОВ
@bot.tree.command(name="principles", description="Показать все принципы правления")
async def principles(interaction: discord.Interaction):
    try:
        principles = await load_data("national_principles.json")
        if not principles:
            await interaction.response.send_message("📜 Принципы не добавлены!", ephemeral=True)
            return

        embed = discord.Embed(
            title="🌸 Национальные принципы",
            color=DEFAULT_COLOR,
            description="Доступно для выбора:"
        )

        # Сортировка по имени и вывод
        sorted_principles = sorted(principles.items(), key=lambda x: x[1]['name'])
        for pid, data in sorted_principles:
            embed.add_field(
                name=f"{data['name']} ({data['cost']} очков)",
                value=f"ID: {pid}",
                inline=False
            )
            if len(embed.fields) % 15 == 0:  # 15 принципов на страницу
                await interaction.channel.send(embed=embed)
                embed = discord.Embed(color=DEFAULT_COLOR)

        await interaction.response.send_message(embed=embed)
    except Exception as e:
        error_logger.error(f"Ошибка principles: {str(e)}")


# ========== СОБЫТИЯ ==========
@bot.event
async def on_message(message):
    if message.author.bot:
        return

    content = message.content.lower()
    is_ping = bot.user.mentioned_in(message)
    user_id = message.author.id

    # Проверка на спам (для всех сообщений)
    if await bot.is_spam(user_id):
        if random.random() < 0.6:  # 60% шанс ответить на спам
            await message.channel.send(random.choice(bot.responses["spam_responses"]))
        return

    # 1. Проверка на матерные слова (только при пинге, согласно вашему handle_swear)
    if is_ping:
        if await bot.handle_swear(message):  # Если был мат в пинге - обработка завершена
            return

    # 2. Игнорируем пустые пинги
    if is_ping and content.replace(f'<@{bot.user.id}>', '').strip() == '':
        return

    # 3. Обработка "спасибо" (с пингом или без)
    thanks_words = ["спасибо", "благодарю", "thanks", "thx"]
    if any(word in content for word in thanks_words):
        await bot.send_emotional_response(message.channel, "thanks_responses", message.author.mention if is_ping else None)
        return

    # 4. Обработка других триггеров
    triggers = {
        "riki": ["рики тупой", "рики еблан", "рики дегенерат"],
        "hleb": ["хлеб", "hleb", "хлеборис"],
        "farewell": ["пока", "до свидания", "давай", "bye"],
        "compl": ["волчица", "умная", "милая", "классная", "красотка", "тигрица", "ахуенная"],
        "morning": ["доброе утро", "утро", "утра"],
        "evening": ["добрый вечер"],
        "night": ["доброй ночи", "спокойной ночи", "спать", "ночи"]
    }

    for response_type, words in triggers.items():
        if any(word in content for word in words):
            await bot.send_emotional_response(message.channel, response_type, message.author.mention if is_ping else None)
            return

    # 5. Обработка обычных пингов (без триггеров)
    if is_ping:
        await bot.send_emotional_response(message.channel, "greetings", message.author.mention)
        return

    # Разрешенные каналы
    allowed_channels = []
    if bot.offtopic_channel:
        allowed_channels.append(bot.offtopic_channel.id)

    if message.channel.id not in allowed_channels and not isinstance(message.channel, discord.Thread):
        return

    # Дополнительные функции
    await bot.maybe_send_joke(message.channel, message.author.mention)
    await bot.maybe_send_flirt(message.channel)
    await bot.maybe_send_dmc(message.channel)
    await bot.send_time_based_greeting(message.channel)
    await bot.process_commands(message)

@bot.event
async def on_ready():
    try:
        synced = await bot.tree.sync()
        logging.info(f"Синхронизировано {len(synced)} команд")

        config = await load_data("bot_config.json")
        for guild in bot.guilds:
            guild_config = config.get(str(guild.id), {})
            if guild_config and "offtopic_channel" in guild_config:
                bot.offtopic_channel = guild.get_channel(guild_config["offtopic_channel"])

    except Exception as e:
        logging.error(f"Ошибка синхронизации команд: {str(e)}")

    activity = discord.Activity(
        type=discord.ActivityType.watching,
        name=f"за {len(bot.guilds)} серверами | Настроение: {bot.mood}"
    )
    await bot.change_presence(activity=activity)
    logging.info(f"Бот {bot.user.name} готов к работе! Настроение: {bot.mood}")




@bot.event
async def on_ready():
    try:
        synced = await bot.tree.sync()
        logging.info(f"Синхронизировано {len(synced)} команд")

        config = await load_data("bot_config.json")
        for guild in bot.guilds:
            guild_config = config.get(str(guild.id), {})
            if guild_config and "offtopic_channel" in guild_config:
                bot.offtopic_channel = guild.get_channel(guild_config["offtopic_channel"])


    except Exception as e:
        logging.error(f"Ошибка синхронизации команд: {str(e)}")

    activity = discord.Activity(
        type=discord.ActivityType.watching,
        name=f"за {len(bot.guilds)} серверами | Настроение: {bot.mood}"
    )
    await bot.change_presence(activity=activity)
    logging.info(f"Бот {bot.user.name} готов к работе! Настроение: {bot.mood}")



# ========== ЗАПУСК БОТА ==========
if __name__ == "__main__":
    if not os.path.exists(BACKUPS_DIR):
        os.makedirs(BACKUPS_DIR)

    for file in DATA_FILES + [ERROR_LOGS_FILE]:
        if not os.path.exists(file):
            with open(file, "w") as f:
                if file.endswith(".json"):
                    json.dump({}, f)

    if not os.path.exists("config.json"):
        with open("config.json", "w") as f:
            json.dump({"token": "YOUR_BOT_TOKEN"}, f, indent=4)
        print("Создан config.json. Добавьте токен!")
        exit()

    with open("config.json") as f:
        config = json.load(f)

    if "token" not in config:
        print("❌ Токен бота не найден в config.json!")
        exit()

    bot.run(config["token"])

